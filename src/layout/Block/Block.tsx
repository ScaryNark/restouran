import React from 'react';
import s from './Block.module.scss'

const Block = () => {
    return (
        <section id={s.block}>
            <h1>Данное поле не заполнено в связи с отсутсвием дизайна на этой странице.</h1>
        </section>
    );
};

export default Block;